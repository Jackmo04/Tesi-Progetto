FROM alpine:latest AS builder

RUN apk add --no-cache \
    bash \
    curl \
    git \
    go \
    unzip \
    make

RUN git clone https://github.com/activeshadow/go-atomicredteam.git /goart && \
    cd /goart && \
    make bin/goart

FROM alpine:latest

RUN apk add --no-cache \
    bash \
    curl \
    sudo \
    wget \
    python3

COPY --from=builder /goart/bin/goart /usr/local/bin/goart

CMD ["goart"]
